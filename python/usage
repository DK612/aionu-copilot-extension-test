# Docker Compose로 빌드 및 실행
docker/docker-compose up --build

# 또는 Python 가상환경으로 직접 실행
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
python app.py